# Django Backend Dependencies for Quizzer Application
# This file has been superseded by pyproject.toml
# To update dependencies:
#   1. Edit dependencies in pyproject.toml
#   2. Run: uv pip compile pyproject.toml -o requirements.lock
#   3. Install: pip install -r requirements.lock
#
# For development, you can still install from this file,
# but the lock file (requirements.lock) is preferred for reproducibility.

# Core Django Framework
Django>=5.1,<5.2

# REST API Support
djangorestframework>=3.14,<4.0

# CORS Headers
django-cors-headers>=4.0,<5.0

# WebSocket Support (Django Channels)
channels>=4.0,<5.0

# ASGI Server for Channels
daphne>=4.0,<5.0

# Color Field Support for Team Colors
django-colorfield>=0.11,<1.0
